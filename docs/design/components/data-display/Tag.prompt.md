Rounded-full chip for link tags and dismissible active-filter pills.

```jsx
<Tag>launch</Tag>
<Tag tone="primary" onRemove={clearStatus}>Status: Active</Tag>
```

`neutral` on link cards/rows; `primary` for the filter bar's active-filter pills.
