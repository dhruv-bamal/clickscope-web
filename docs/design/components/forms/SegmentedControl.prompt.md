Equal-width segmented track for a small exclusive choice — theme mode, analytics time range.

```jsx
<SegmentedControl value={range} onChange={setRange}
  options={["24h", "7d", "30d"]} aria-label="Time range" />
```
