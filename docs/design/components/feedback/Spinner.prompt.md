Loading indicator for actions with no predictable content (button loading, alias check).

```jsx
<Spinner size={16} />
```

Never use a full-page spinner for a data screen — use `Skeleton` blocks that match the layout instead.
