Placeholder block for known-shape content that is loading. Match the real layout's boxes to avoid shift.

```jsx
<Skeleton width={120} height={28} />
<Skeleton height={12} radius={4} />
```

Use for initial page/region load (stat cards, tables, charts). Use `Spinner` for button/action loading.
